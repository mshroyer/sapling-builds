diff --git a/eden/scm/lib/indexedlog/Cargo.toml b/eden/scm/lib/indexedlog/Cargo.toml
--- a/eden/scm/lib/indexedlog/Cargo.toml
+++ b/eden/scm/lib/indexedlog/Cargo.toml
@@ -38,6 +38,7 @@
 memmap2 = "0.9.11"
 rand = "0.10.2"
 sapling-atomicfile = { version = "0.1.0", path = "../atomicfile" }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-configmodel = { version = "0.1.0", path = "../config/model", optional = true }
 sapling-metrics = { version = "0.1.0", path = "../metrics", optional = true }
 sapling-minibytes = { version = "0.1.0", path = "../minibytes" }
diff --git a/eden/scm/lib/indexedlog/src/lib.rs b/eden/scm/lib/indexedlog/src/lib.rs
--- a/eden/scm/lib/indexedlog/src/lib.rs
+++ b/eden/scm/lib/indexedlog/src/lib.rs
@@ -6,7 +6,6 @@
  */
 
 #![allow(dead_code)]
-#![feature(once_cell_try)]
 
 //! # Indexed Log
 //!
diff --git a/eden/scm/lib/indexedlog/src/rotate.rs b/eden/scm/lib/indexedlog/src/rotate.rs
--- a/eden/scm/lib/indexedlog/src/rotate.rs
+++ b/eden/scm/lib/indexedlog/src/rotate.rs
@@ -884,7 +884,7 @@
             Some(cell) => {
                 let id = self.latest.wrapping_sub(index as u8);
                 if let Some(dir) = &self.dir {
-                    let log = cell.get_or_try_init(|| {
+                    let log = compilerstubs::get_or_try_init(cell, || {
                         let mut open_options = self.open_options.log_open_options.clone();
                         if index > 0 {
                             open_options = open_options.with_zero_index_lag();
diff --git a/eden/scm/lib/manifest-tree/Cargo.toml b/eden/scm/lib/manifest-tree/Cargo.toml
--- a/eden/scm/lib/manifest-tree/Cargo.toml
+++ b/eden/scm/lib/manifest-tree/Cargo.toml
@@ -25,6 +25,7 @@
 parking_lot = { version = "0.12.1", features = ["arc_lock", "send_guard"] }
 quickcheck = { version = "1.1", optional = true }
 rand = { version = "0.10.2", optional = true }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-edenapi_types = { version = "0.1.0", path = "../edenapi/types" }
 sapling-factory = { version = "0.1.0", path = "../util/factory" }
 sapling-format-util = { version = "0.1.0", path = "../util/format-util" }
diff --git a/eden/scm/lib/manifest-tree/src/lib.rs b/eden/scm/lib/manifest-tree/src/lib.rs
--- a/eden/scm/lib/manifest-tree/src/lib.rs
+++ b/eden/scm/lib/manifest-tree/src/lib.rs
@@ -5,8 +5,6 @@
  * LICENSE file in the root directory of this source tree.
  */
 
-#![feature(once_cell_try)]
-
 mod acl_metrics;
 mod bfs;
 mod diff;
diff --git a/eden/scm/lib/manifest-tree/src/link.rs b/eden/scm/lib/manifest-tree/src/link.rs
--- a/eden/scm/lib/manifest-tree/src/link.rs
+++ b/eden/scm/lib/manifest-tree/src/link.rs
@@ -253,7 +253,7 @@
         store: &InnerStore,
         path: &RepoPath,
     ) -> Result<&BTreeMap<PathComponentBuf, Link>> {
-        let maybe = self.links.get_or_try_init(|| -> Result<MaybeLinks> {
+        let maybe = compilerstubs::get_or_try_init(&self.links, || -> Result<MaybeLinks> {
             let tree_entry = match store.get_tree_entry(path, self.hgid) {
                 Ok(entry) => entry,
                 Err(err) => match err.downcast::<types::errors::PermissionDenied>() {
@@ -262,7 +262,7 @@
                         return Err(err.context(format!(
                             "failed fetching from store ({}, {})",
                             path, self.hgid
-                        )))
+                        )));
                     }
                 },
             };
@@ -279,9 +279,7 @@
                     )
                 })?;
                 let link = match flag {
-                    TreeItemFlag::File(file_type) => {
-                        Link::leaf(FileMetadata::new(hgid, file_type))
-                    }
+                    TreeItemFlag::File(file_type) => Link::leaf(FileMetadata::new(hgid, file_type)),
                     TreeItemFlag::Directory => Link::durable(hgid),
                 };
                 links.insert(component.to_owned(), link);
diff --git a/eden/scm/lib/metrics/Cargo.toml b/eden/scm/lib/metrics/Cargo.toml
--- a/eden/scm/lib/metrics/Cargo.toml
+++ b/eden/scm/lib/metrics/Cargo.toml
@@ -17,6 +17,7 @@
 fastrand = "2.4.1"
 futures = { version = "0.3.31", features = ["async-await", "compat"] }
 parking_lot = { version = "0.12.1", features = ["arc_lock", "send_guard"] }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 
 [features]
 default = []
diff --git a/eden/scm/lib/metrics/src/ods.rs b/eden/scm/lib/metrics/src/ods.rs
--- a/eden/scm/lib/metrics/src/ods.rs
+++ b/eden/scm/lib/metrics/src/ods.rs
@@ -60,7 +60,7 @@
             "Failed to initialize obc client for logging to ods"
         ));
     }
-    OBC_CLIENT.get_or_try_init(|| -> anyhow::Result<OBCClientWrapper> {
+    compilerstubs::get_or_try_init(&OBC_CLIENT, || -> anyhow::Result<OBCClientWrapper> {
         let opts = OBCClientOptions::builder()
             .ods_category("eden")
             .append_agg_type_suffix(false)
diff --git a/eden/scm/lib/repo/Cargo.toml b/eden/scm/lib/repo/Cargo.toml
--- a/eden/scm/lib/repo/Cargo.toml
+++ b/eden/scm/lib/repo/Cargo.toml
@@ -21,6 +21,7 @@
 sapling-async-runtime = { version = "0.1.0", path = "../async-runtime" }
 sapling-commits = { version = "0.1.0", path = "../commits" }
 sapling-commits-trait = { version = "0.1.0", path = "../commits/trait" }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-configloader = { version = "0.1.0", path = "../config/loader" }
 sapling-configmodel = { version = "0.1.0", path = "../config/model" }
 sapling-constructors = { version = "0.1.0", path = "../constructors", default-features = false }
diff --git a/eden/scm/lib/repo/src/lib.rs b/eden/scm/lib/repo/src/lib.rs
--- a/eden/scm/lib/repo/src/lib.rs
+++ b/eden/scm/lib/repo/src/lib.rs
@@ -6,7 +6,6 @@
  */
 
 #![allow(dead_code)]
-#![feature(once_cell_try)]
 
 pub mod constants;
 pub mod core_repo;
diff --git a/eden/scm/lib/repo/src/repo.rs b/eden/scm/lib/repo/src/repo.rs
--- a/eden/scm/lib/repo/src/repo.rs
+++ b/eden/scm/lib/repo/src/repo.rs
@@ -422,7 +422,7 @@
     }
 
     pub fn tree_resolver(&self) -> Result<Arc<dyn ReadTreeManifest + Send + Sync>> {
-        let tr = self.tree_resolver.get_or_try_init(|| {
+        let tr = compilerstubs::get_or_try_init(&self.tree_resolver, || {
             let tree_store = self.tree_store()?;
             let local: Arc<dyn ReadTreeManifest + Send + Sync> = Arc::new(LocalTreeResolver::new(
                 self.dag_commits()?,
diff --git a/eden/scm/lib/repo/src/slapi_client.rs b/eden/scm/lib/repo/src/slapi_client.rs
--- a/eden/scm/lib/repo/src/slapi_client.rs
+++ b/eden/scm/lib/repo/src/slapi_client.rs
@@ -33,8 +33,9 @@
         &self,
         eden_api: Arc<dyn SaplingRemoteApi>,
     ) -> Result<&Capabilities, SaplingRemoteApiError> {
-        self.caps
-            .get_or_try_init(|| block_on(eden_api.capabilities()).map(|c| c.into_iter().collect()))
+        compilerstubs::get_or_try_init(&self.caps, || {
+            block_on(eden_api.capabilities()).map(|c| c.into_iter().collect())
+        })
     }
 }
 
@@ -47,7 +48,8 @@
     once_slapi: &OnceSlapi,
     repo_url: Option<RepoUrl>,
 ) -> Result<(LazyCapabilities, Arc<dyn SaplingRemoteApi>), SaplingRemoteApiError> {
-    let (caps, eden_api) = once_slapi.get_or_try_init(
+    let (caps, eden_api) = compilerstubs::get_or_try_init(
+        &once_slapi,
         || -> Result<(LazyCapabilities, Arc<dyn SaplingRemoteApi>), SaplingRemoteApiError> {
             tracing::trace!(target: "repo::eden_api", "creating edenapi");
             let mut builder = Builder::from_config(config)?;
diff --git a/eden/scm/lib/repo/src/slapi_repo.rs b/eden/scm/lib/repo/src/slapi_repo.rs
--- a/eden/scm/lib/repo/src/slapi_repo.rs
+++ b/eden/scm/lib/repo/src/slapi_repo.rs
@@ -141,7 +141,7 @@
 
     /// Get the tree resolver, constructing it if necessary.
     pub fn tree_resolver(&self) -> Result<Arc<dyn ReadTreeManifest + Send + Sync>> {
-        let tr = self.tree_resolver.get_or_try_init(|| {
+        let tr = compilerstubs::get_or_try_init(&self.tree_resolver, || {
             Ok::<_, anyhow::Error>(Arc::new(SlapiTreeResolver::new(
                 self.eden_api()?,
                 self.tree_store()?,
diff --git a/eden/scm/lib/revisionstore/Cargo.toml b/eden/scm/lib/revisionstore/Cargo.toml
--- a/eden/scm/lib/revisionstore/Cargo.toml
+++ b/eden/scm/lib/revisionstore/Cargo.toml
@@ -31,6 +31,7 @@
 rand = "0.10.2"
 redacted = { version = "0.1.0", path = "../util/redacted" }
 sapling-async-runtime = { version = "0.1.0", path = "../async-runtime" }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-configmodel = { version = "0.1.0", path = "../config/model" }
 sapling-edenapi = { version = "0.1.0", path = "../edenapi" }
 sapling-edenapi_types = { version = "0.1.0", path = "../edenapi/types" }
diff --git a/eden/scm/lib/revisionstore/src/indexedlogdatastore.rs b/eden/scm/lib/revisionstore/src/indexedlogdatastore.rs
--- a/eden/scm/lib/revisionstore/src/indexedlogdatastore.rs
+++ b/eden/scm/lib/revisionstore/src/indexedlogdatastore.rs
@@ -192,7 +192,7 @@
     }
 
     pub(crate) fn calculate_content(&self) -> Result<Bytes> {
-        let content = self.content.get_or_try_init(|| {
+        let content = compilerstubs::get_or_try_init(&self.content, || {
             if let Some(compressed) = self.compressed_content.as_ref() {
                 let raw = Bytes::from(decompress(compressed)?);
                 Ok(raw)
diff --git a/eden/scm/lib/revisionstore/src/lib.rs b/eden/scm/lib/revisionstore/src/lib.rs
--- a/eden/scm/lib/revisionstore/src/lib.rs
+++ b/eden/scm/lib/revisionstore/src/lib.rs
@@ -97,7 +97,6 @@
 //! The produced stores must implement the `HgIdDataStore` trait.
 
 #![allow(unexpected_cfgs)]
-#![feature(once_cell_try)]
 
 mod indexedloghistorystore;
 mod lfs;
diff --git a/eden/scm/lib/revisionstore/src/scmstore/tree.rs b/eden/scm/lib/revisionstore/src/scmstore/tree.rs
--- a/eden/scm/lib/revisionstore/src/scmstore/tree.rs
+++ b/eden/scm/lib/revisionstore/src/scmstore/tree.rs
@@ -161,10 +161,10 @@
     pub fn aux_data(&self) -> Result<Option<TreeAuxData>> {
         let node = self.node();
         let tree_aux_store = self.tree_aux_store.clone();
-        Ok(self
-            .tree_aux
-            .get_or_try_init(|| get_local_aux_direct(tree_aux_store.as_deref(), &node))?
-            .clone())
+        Ok(compilerstubs::get_or_try_init(&self.tree_aux, || {
+            get_local_aux_direct(tree_aux_store.as_deref(), &node)
+        })?
+        .clone())
     }
 }
 
@@ -1117,9 +1117,10 @@
 
 impl ScmStoreTreeEntry {
     fn basic_tree_entry(&self) -> Result<&dyn TreeEntry> {
-        self.basic_tree_entry
-            .get_or_try_init(|| Ok(Arc::new(self.tree.manifest_tree_entry()?)))
-            .map(Borrow::borrow)
+        compilerstubs::get_or_try_init(&self.basic_tree_entry, || {
+            Ok(Arc::new(self.tree.manifest_tree_entry()?))
+        })
+        .map(Borrow::borrow)
     }
 }
 
diff --git a/eden/scm/lib/storemodel/Cargo.toml b/eden/scm/lib/storemodel/Cargo.toml
--- a/eden/scm/lib/storemodel/Cargo.toml
+++ b/eden/scm/lib/storemodel/Cargo.toml
@@ -18,6 +18,7 @@
 blob = { version = "0.1.0", path = "../blob" }
 futures = { version = "0.3.31", features = ["async-await", "compat"] }
 parking_lot = { version = "0.12.1", features = ["arc_lock", "send_guard"] }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-configmodel = { version = "0.1.0", path = "../config/model" }
 sapling-edenapi_trait = { version = "0.1.0", path = "../edenapi/trait" }
 sapling-edenapi_types = { version = "0.1.0", path = "../edenapi/types" }
diff --git a/eden/scm/lib/storemodel/src/lib.rs b/eden/scm/lib/storemodel/src/lib.rs
--- a/eden/scm/lib/storemodel/src/lib.rs
+++ b/eden/scm/lib/storemodel/src/lib.rs
@@ -20,8 +20,6 @@
 //! Traits can be combined later. For example, reading file content, metadata,
 //! and history should probably be 3 different traits.
 
-#![feature(once_cell_try)]
-
 use std::any::Any;
 use std::any::type_name;
 use std::borrow::Cow;
@@ -679,8 +677,9 @@
 ) -> anyhow::Result<Arc<dyn TreeEntry>> {
     // Only call `call_constructor` once to avoid overhead in `factory`.
     static TREE_PARSER: OnceLock<StaticSerializedTreeParseFunc> = OnceLock::new();
-    let parse = TREE_PARSER
-        .get_or_try_init(|| factory::call_constructor::<(), StaticSerializedTreeParseFunc>(&()))?;
+    let parse = compilerstubs::get_or_try_init(&TREE_PARSER, || {
+        factory::call_constructor::<(), StaticSerializedTreeParseFunc>(&())
+    })?;
     parse(data, format)
 }
 
@@ -691,8 +690,9 @@
 ) -> anyhow::Result<Bytes> {
     // Only call `call_constructor` once to avoid overhead in `factory`.
     static TREE_SERIALIZER: OnceLock<StaticSerializeTreeFunc> = OnceLock::new();
-    let serialize = TREE_SERIALIZER
-        .get_or_try_init(|| factory::call_constructor::<(), StaticSerializeTreeFunc>(&()))?;
+    let serialize = compilerstubs::get_or_try_init(&TREE_SERIALIZER, || {
+        factory::call_constructor::<(), StaticSerializeTreeFunc>(&())
+    })?;
     serialize(items, format)
 }
 
diff --git a/eden/scm/lib/util/compilerstubs/Cargo.toml b/eden/scm/lib/util/compilerstubs/Cargo.toml
new file mode 100644
--- /dev/null
+++ b/eden/scm/lib/util/compilerstubs/Cargo.toml
@@ -0,0 +1,9 @@
+[package]
+name = "sapling-compilerstubs"
+version = "0.1.0"
+edition = "2024"
+
+[lib]
+name = "compilerstubs"
+
+[dependencies]
diff --git a/eden/scm/lib/util/compilerstubs/compilerstubs/Cargo.toml b/eden/scm/lib/util/compilerstubs/compilerstubs/Cargo.toml
new file mode 100644
--- /dev/null
+++ b/eden/scm/lib/util/compilerstubs/compilerstubs/Cargo.toml
@@ -0,0 +1,9 @@
+[package]
+name = "sapling-compilerstubs"
+version = "0.1.0"
+edition = "2024"
+
+[lib]
+name = "compilerstubs"
+
+[dependencies]
diff --git a/eden/scm/lib/util/compilerstubs/compilerstubs/src/lib.rs b/eden/scm/lib/util/compilerstubs/compilerstubs/src/lib.rs
new file mode 100644
--- /dev/null
+++ b/eden/scm/lib/util/compilerstubs/compilerstubs/src/lib.rs
@@ -0,0 +1,15 @@
+//! Workarounds for missing stable compiler features.
+
+use std::result::Result;
+use std::sync::OnceLock;
+
+pub fn get_or_try_init<T, F, E>(cell: &OnceLock<T>, f: F) -> Result<&T, E>
+where
+    F: FnOnce() -> Result<T, E>,
+{
+    if let Some(v) = cell.get() {
+        return Ok(v);
+    }
+    let v = f()?;
+    Ok(cell.get_or_init(|| v))
+}
diff --git a/eden/scm/lib/util/compilerstubs/src/lib.rs b/eden/scm/lib/util/compilerstubs/src/lib.rs
new file mode 100644
--- /dev/null
+++ b/eden/scm/lib/util/compilerstubs/src/lib.rs
@@ -0,0 +1,15 @@
+//! Workarounds for missing stable compiler features.
+
+use std::result::Result;
+use std::sync::OnceLock;
+
+pub fn get_or_try_init<T, F, E>(cell: &OnceLock<T>, f: F) -> Result<&T, E>
+where
+    F: FnOnce() -> Result<T, E>,
+{
+    if let Some(v) = cell.get() {
+        return Ok(v);
+    }
+    let v = f()?;
+    Ok(cell.get_or_init(|| v))
+}
diff --git a/eden/scm/lib/util/format-util/Cargo.toml b/eden/scm/lib/util/format-util/Cargo.toml
--- a/eden/scm/lib/util/format-util/Cargo.toml
+++ b/eden/scm/lib/util/format-util/Cargo.toml
@@ -15,6 +15,7 @@
 [dependencies]
 anyhow = "1.0.103"
 memchr = "2.8.3"
+sapling-compilerstubs = { version = "0.1.0", path = "../compilerstubs" }
 sapling-hgtime = { version = "0.1.0", path = "../../hgtime" }
 sapling-minibytes = { version = "0.1.0", path = "../../minibytes" }
 sapling-types = { version = "0.1.0", path = "../../types" }
diff --git a/eden/scm/lib/util/format-util/src/git_commit_fields.rs b/eden/scm/lib/util/format-util/src/git_commit_fields.rs
--- a/eden/scm/lib/util/format-util/src/git_commit_fields.rs
+++ b/eden/scm/lib/util/format-util/src/git_commit_fields.rs
@@ -187,9 +187,9 @@
     }
 
     pub fn fields(&self) -> Result<&GitCommitFields> {
-        let fields = self
-            .fields
-            .get_or_try_init(|| GitCommitFields::from_text(&self.text).map(Box::new))?;
+        let fields = compilerstubs::get_or_try_init(&self.fields, || {
+            GitCommitFields::from_text(&self.text).map(Box::new)
+        })?;
         Ok(fields)
     }
 }
diff --git a/eden/scm/lib/util/format-util/src/hg_commit_fields.rs b/eden/scm/lib/util/format-util/src/hg_commit_fields.rs
--- a/eden/scm/lib/util/format-util/src/hg_commit_fields.rs
+++ b/eden/scm/lib/util/format-util/src/hg_commit_fields.rs
@@ -187,9 +187,9 @@
     }
 
     pub fn fields(&self) -> Result<&HgCommitFields> {
-        let fields = self
-            .fields
-            .get_or_try_init(|| HgCommitFields::from_text(&self.text).map(Box::new))?;
+        let fields = compilerstubs::get_or_try_init(&self.fields, || {
+            HgCommitFields::from_text(&self.text).map(Box::new)
+        })?;
         Ok(fields)
     }
 }
diff --git a/eden/scm/lib/util/format-util/src/lib.rs b/eden/scm/lib/util/format-util/src/lib.rs
--- a/eden/scm/lib/util/format-util/src/lib.rs
+++ b/eden/scm/lib/util/format-util/src/lib.rs
@@ -7,8 +7,6 @@
 
 //! Utilities interacting with store serialization formats (git or hg).
 
-#![feature(once_cell_try)]
-
 use anyhow::Result;
 pub use minibytes::Bytes;
 use minibytes::Text;
diff --git a/eden/scm/lib/util/rewrite-macros/Cargo.toml b/eden/scm/lib/util/rewrite-macros/Cargo.toml
--- a/eden/scm/lib/util/rewrite-macros/Cargo.toml
+++ b/eden/scm/lib/util/rewrite-macros/Cargo.toml
@@ -19,6 +19,7 @@
 [dependencies]
 proc-macro2 = { version = "1.0.106", features = ["span-locations"] }
 quote = "1.0.46"
+sapling-compilerstubs = { version = "0.1.0", path = "../compilerstubs" }
 sapling-tree-pattern-match = { version = "0.1.0", path = "tree-pattern-match" }
 
 [dev-dependencies]
diff --git a/eden/scm/lib/util/rewrite-macros/src/cached.rs b/eden/scm/lib/util/rewrite-macros/src/cached.rs
--- a/eden/scm/lib/util/rewrite-macros/src/cached.rs
+++ b/eden/scm/lib/util/rewrite-macros/src/cached.rs
@@ -47,7 +47,7 @@
             quote! {
                 pub fn #name(&self) -> Result<#ret_type> {
                     ::tracing::trace!(stringify!(#name));
-                    Ok(self.#name.get_or_try_init(|| -> Result<#ret_type> {
+                    Ok(compilerstubs::get_or_try_init(&self.#name, || -> Result<#ret_type> {
                         let v = Arc::new(RwLock::new(self.#load_name()?));
                         #post_load
                         Ok(v)
diff --git a/eden/scm/lib/workingcopy/Cargo.toml b/eden/scm/lib/workingcopy/Cargo.toml
--- a/eden/scm/lib/workingcopy/Cargo.toml
+++ b/eden/scm/lib/workingcopy/Cargo.toml
@@ -20,6 +20,7 @@
 parking_lot = { version = "0.12.1", features = ["arc_lock", "send_guard"] }
 regex = "1.13.1"
 sapling-async-runtime = { version = "0.1.0", path = "../async-runtime" }
+sapling-compilerstubs = { version = "0.1.0", path = "../util/compilerstubs" }
 sapling-configmodel = { version = "0.1.0", path = "../config/model", features = ["convert-path"] }
 sapling-context = { version = "0.1.0", path = "../context" }
 sapling-edenfs-client = { version = "0.1.0", path = "../edenfs-client", optional = true }
diff --git a/eden/scm/lib/workingcopy/src/lib.rs b/eden/scm/lib/workingcopy/src/lib.rs
--- a/eden/scm/lib/workingcopy/src/lib.rs
+++ b/eden/scm/lib/workingcopy/src/lib.rs
@@ -5,8 +5,6 @@
  * LICENSE file in the root directory of this source tree.
  */
 
-#![feature(once_cell_try)]
-
 pub mod client;
 pub mod config;
 mod errors;
diff --git a/eden/scm/lib/workingcopy/src/watchman_client.rs b/eden/scm/lib/workingcopy/src/watchman_client.rs
--- a/eden/scm/lib/workingcopy/src/watchman_client.rs
+++ b/eden/scm/lib/workingcopy/src/watchman_client.rs
@@ -31,8 +31,7 @@
     }
 
     pub fn get(&self) -> Result<Arc<watchman_client::Client>> {
-        self.watchman_client
-            .get_or_try_init(|| connect_watchman(&self.config))
+        compilerstubs::get_or_try_init(&self.watchman_client, || connect_watchman(&self.config))
             .cloned()
     }
 }
