#!/bin/sh

# thrift1 wrapper to ensure include paths needed by the sapling build are
# specified.

PREFIX="/opt/fbthrift"
GETDEPS_INSTALLED="/tmp/fbcode_builder_getdeps-ZfbthriftZbuildZfbcode_builder-root/installed"

exec "${PREFIX}/bin/thrift1" -I "${PREFIX}/include" $@
