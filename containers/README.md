# Containers for building/smoketesting sapling and fbthrift

These are best invoked via the build and smoketest scripts.  The distro-specific sapling-builder containers need the shared "files" directory copied into them before building, which the scripts handle.
